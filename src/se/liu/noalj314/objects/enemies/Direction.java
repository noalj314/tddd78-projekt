package se.liu.noalj314.objects.enemies;
/**
 * The Direction enum represents the direction an enemy can move in the game.
 * It can be RIGHT, LEFT, UP, DOWN, or FIRST.
 */
public enum Direction
{
    RIGHT, LEFT, UP, DOWN, FIRST
}
